option( BUILD_PERFUSION "Build the Perfusion Programs" ON )
if( ${BUILD_MT} )
    file(GLOB SOURCES *.cpp)
    target_sources(qi PRIVATE ${SOURCES})
    target_compile_definitions(qi PRIVATE "-DBUILD_PERFUSION")
endif()