option( BUILD_B1 "Build the B1+/- programs" ON )
if( ${BUILD_B1} )
    file(GLOB SOURCES *.cpp)
    target_sources(qi PRIVATE ${SOURCES})
    target_compile_definitions(qi PRIVATE "-DBUILD_B1")
endif()