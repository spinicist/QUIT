option( BUILD_SUSCEP "Build the susceptibility programs" ON )
if( ${BUILD_SUSCEP} )
    file(GLOB SOURCES *.cpp)
    target_sources(qi PRIVATE ${SOURCES})
    target_compile_definitions(qi PRIVATE "-DBUILD_SUSCEP")
endif()