option( BUILD_STATS "Build the Stats Utilities" ON )
if( ${BUILD_STATS} )
    file(GLOB SOURCES *.cpp)
    target_sources(qi PRIVATE ${SOURCES})
    target_compile_definitions(qi PRIVATE "-DBUILD_STATS")
endif()