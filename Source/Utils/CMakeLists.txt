option( BUILD_UTIL "Build the utility programs" ON )
if( ${BUILD_UTIL} )
    file(GLOB SOURCES *.cpp)
    target_sources(qi PRIVATE ${SOURCES})
    target_compile_definitions(qi PRIVATE "-DBUILD_UTILS")
endif()